# Supabase Configuration
# Get these from: https://app.supabase.com → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Magic Link Redirect URL (for Authentication)
# For local Docker development, use: http://localhost:4173
# Make sure this URL is registered in Supabase Auth → URL Configuration
VITE_MAGIC_LINK_REDIRECT_URL=http://localhost:4173

# Development-only: Auth Bypass for local development
# Set to 'true' to login without Magic Link emails
# Only works when: import.meta.env.DEV === true (never in production)
# Usage: Set VITE_AUTH_BYPASS=true in frontend/.env.local
VITE_AUTH_BYPASS=false

# Status monitoring (Uptime Kuma) — used by the in-app status banner.
# Uptime Kuma is deployed independently of the client (see deploy/monitoring),
# so the client polls its status API directly on the status subdomain. That way
# health is still reported when the client server itself is down.
# Slug of the public Uptime Kuma status page (default: campus).
VITE_STATUS_PAGE_SLUG=campus
# Public URL of the status page, shown as a link in the banner.
VITE_STATUS_PAGE_URL=https://status.your-domain/status/campus
# Base URL of Kuma's status-page JSON API, on the status subdomain (NOT proxied
# through the client). Leave empty only for a same-origin/dev setup, in which
# case it falls back to /api/status-page. The status vhost must send CORS
# headers for the app origin (see deploy/README.md).
VITE_STATUS_API_BASE=https://status.your-domain/api/status-page
